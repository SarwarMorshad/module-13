/*
!!variable > 5 things to declare variables in JavaScript
* 1. variable name
* 2. variable value
* 3. variable type
*/

var year = 2023; // variable name, value, type (number)
let name = "John"; // variable name, value, type (string)
const pi = 3.14; // variable name, value, type (number)

var age; // variable name, type (undefined)

// console.log(age);

/*
!!variable > Naming conventions
* 1. No keywords (e.g., var, let, const, function, etc.)
* 2. No spaces (e.g., my variable)
* 3. No special characters (e.g., @, #, $, %, etc.)
* 4. Name is case-sensitive (e.g., myVar vs myvar)
* 5. Start with a letter or underscore (e.g., _myVar, myVar)
*    - Avoid starting with a number (e.g., 1myVar is invalid)
* 6. Use snake_Case/camelCase for multi-word names (e.g., my_variable, myVariable)
*/
